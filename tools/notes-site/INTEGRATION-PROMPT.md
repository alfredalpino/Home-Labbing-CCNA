# Integration prompt — mount CCNA notes at alubaid.xyz/notes

> Prefer the package copy: `ubaid-notes/PROMPT.md` (paste that into the UBAID-Site chat).

If the folder `ubaid-notes/` is already inside UBAID-Site:

```text
ubaid-notes/site/  →  public/notes/
```

Design: IBM Plex Sans/Mono · signal teal · `data-theme` + `localStorage.theme` · no homepage link · keep `/wire`.
