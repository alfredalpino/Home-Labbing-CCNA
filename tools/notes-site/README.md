# Notes site builder

Builds `ubaid-notes/site/` for drop-in to alubaid.xyz.

```bash
tools/notes-site/.venv/bin/python tools/notes-site/build.py --base /notes
```

Package to copy into UBAID-Site: **`ubaid-notes/`** (see `ubaid-notes/PROMPT.md`).
