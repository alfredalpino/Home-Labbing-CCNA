# AI — context + logs

Single home for AI-related vault tooling (not CCNA curriculum).

| Path | Purpose |
|------|---------|
| [`context/`](context/) | Identity, goals, roadmap, guardrails, prompt templates |
| [`logs/`](logs/) | Permanent Cursor Q&A archive (`a.txt`, per-session dumps, scripts) |

**Cursor:** `@ai/context` for guardrails · logging rule writes to `ai/logs/`  
**Obsidian:** hide `-path:ai` from the graph so study notes stay clean.
